# Dead Everon Workbench Playtest

The Dead Everon authoring surface moved into the sibling overlay addon:

- `X:\OpenClaw\workspace\projects\reforger-zombie-director-dead-everon-overlay`

Use that project for World Editor work and playtest notes instead of the core
director addon. The current overlay playtest guide lives at:

- `X:\OpenClaw\workspace\projects\reforger-zombie-director-dead-everon-overlay\docs\dead-everon-workbench-playtest.md`

Why it moved:

- the core addon stays map-agnostic again
- the Dead Everon dependency chain is isolated to the overlay addon
- the default authoring world now uses the healthier map parent instead of the noisier Game Master save
